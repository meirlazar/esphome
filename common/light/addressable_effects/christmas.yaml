addressable_lambda:
  name: "Christmas RedGreen"
  lambda: |-
    for (int i = 1; i <  it.size(); i+=2) {
    it[i] = esphome::Color(255, 0, 0);
    }
    for (int i = 0; i <  it.size(); i+=2) {
    it[i] = esphome::Color(0, 255, 0);
    }